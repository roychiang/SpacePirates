{
  "name": "proving_ground",
  "version": "1.0.0",
  "description": "",
  "private": "true",
  "scripts": {
    "recursive-install": "concurrently \"cd app_package && npm install\" \"cd server && npm install\" \"cd test_package && npm install\"",
    "dev": "concurrently \"npm --prefix ./app_package run dev\" \"npm --prefix ./test_package run dev\"",
    "build": "npm --prefix ./app_package run build && npm --prefix ./test_package run build && npm --prefix ./server run build"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^6.2.1",
    "recursive-install": "^1.4.0"
  }
}